services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      # - OPENAI_API_KEY
      # - PUBLIC_DATA_PORTAL_API_KEY_DECODING
      # - PUBLIC_DATA_PORTAL_API_KEY_ENCODING
      # - DATABASE_PWD
      # - DATABASE_SCHEMA
      # - DATABASE_HOST
    env_file:
      - .env.production.local
    # volumes:
    #   - ./.env.production.local:/app/.env.production.local:ro
    # depends_on:
    #   - db
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # db:
  #   image: mysql:latest
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
  #     MYSQL_DATABASE: ${DB_NAME}
  #     MYSQL_USER: ${DB_USER}
  #     MYSQL_PASSWORD: ${DB_PASSWORD}
  #   ports:
  #     - "3306:3306"
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #   restart: unless-stopped

# volumes:
#   mysql_data: 9bdffcdc53e26caaf9efd99a5f1ccd47871d1536a4aa28c9bb22c75b546150e2